function add (a=0, b=0) { //초기값 선언//
    return a + b
}

console.log(add(5,6));

// NaN = Not a Number.

const sum = function(a, b) {
    return a + b
}

console.log(sum(5,5))
console.log(window);

// arrow function () => {}

const sum = (a) => {
    return a + 5
}

const sum = a => a + 5; //짧게 쓰기

